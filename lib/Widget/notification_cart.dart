import 'package:flutter/material.dart';

class NotificationCartWidget extends StatelessWidget {
  const NotificationCartWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // Empty for now
  }
}